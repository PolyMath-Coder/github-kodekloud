name: The very first workflow
on: push
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world action step
        run: echo "Hello world!"

      - name: list files
        run: ls

      - name: Read files
        run: cat README.md
